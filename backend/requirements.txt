fastapi>=0.115
uvicorn[standard]>=0.32
sqlmodel>=0.0.22
httpx>=0.27
aiotractive>=0.5.6

# Not on PyPI - install per its own README, then set FMDN_ENABLED=true and
# FMDN_SECRETS_PATH to point at the resulting Auth/secrets.json:
#   pip install git+https://github.com/leonboe1/GoogleFindMyTools.git
